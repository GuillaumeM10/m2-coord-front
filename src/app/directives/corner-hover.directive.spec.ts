import { CornerHoverDirective } from './corner-hover.directive';

describe('CornerHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new CornerHoverDirective();
    expect(directive).toBeTruthy();
  });
});
